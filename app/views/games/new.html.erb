<h1>New game</h1>

<h2><% @letters.each do |letter| %>
<%= letter %>
<% end %>
<h2>

<h3>What is the longest word you can think of?</h3>
<form action="/score" method="post" data-turbo="false">
  <input class="w-100 form-control" type="text" name="guess" value="<%= params[:guess] %>" placeholder="Type your word...">
  <input class="btn btn-primary" type="submit" value="Ask!">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters %>
</form>
